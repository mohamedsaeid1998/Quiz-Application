const Skeleton = ({ className }) => {
    return (
      <div className={`bg-gray-200 animate-pulse rounded-md ${className}`}>
        {/* Adjust the skeleton appearance based on your UI design */}
      </div>
    );
  };
  export default Skeleton